upstream qwertycoin_wallet_rpc_backend {
    # Keep this separate from qwertycoin_explorer_backend. A frontend rollback
    # must not bypass the parser-based wallet RPC policy or break wallet sync.
    server 127.0.0.1:29984;
    keepalive 8;
}
